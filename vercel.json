{
  "version": 2,
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".build/release"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/.build/release/runespoor"
    }
  ]
}
